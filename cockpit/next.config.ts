import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standalone Next.js app: the MCv2 operator cockpit. Its own Vercel project
  // (none provisioned yet — see README). Nothing portfolio-specific here.
  reactStrictMode: true,
};

export default nextConfig;
