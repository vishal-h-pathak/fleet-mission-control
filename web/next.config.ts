import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standalone Next.js app deployed to its own Vercel project
  // (fleet.vishal.pa.thak.io). Nothing portfolio-specific here.
  reactStrictMode: true,
};

export default nextConfig;
